pipeline {
    agent any
    tools {
        maven 'Maven 3.9.1'
        jdk 'jdk9'
    }
    stages {
        stage ('Build') {
            steps {
                echo 'This is a minimal pipeline.'
            }
        }
    }
}
