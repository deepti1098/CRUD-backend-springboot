pipeline {
    agent any
    tools {
        maven 'Maven'
        jdk 'jdk9'
    }
    stages {
        stage ('Build') {
            steps {
                echo 'This is a minimal pipeline.'
            }
        }
    }
}
