pipeline {
    agent any
    tools {
        maven 'Maven'
        jdk 'jdk'
    }
    stages {
        stage ('Build') {
            steps {
                echo 'This is a minimal pipeline.'
            }
        }
    }
}
